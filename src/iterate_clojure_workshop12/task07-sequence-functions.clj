(ns iterate-clojure-workshop12.task07-seq-fun)

;; map
;; Use the + function to sum two lists '(1 2 3) and '(2 2 2) to (the expected result being '(3 4 5))

; TODO

;; reduce
;; Solve 1 + 2 + 3 using reduce and not (+ 1 2 3).

; TODO


;; apply
;; You can find out the maximum of numbers using (max 2 6 4)
;; but here you got the collection '(2 6 4) containing the numbers

; TODO

;; Use map and apply to find how old the oldest person in the list is in this map:
;; '({ :name "Lars", :age 25 }, { :name "Jakub", :age 32 })

; TODO